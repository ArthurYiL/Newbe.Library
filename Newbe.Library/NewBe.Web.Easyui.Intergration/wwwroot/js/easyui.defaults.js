$.extend($.fn.datagrid.defaults,{
    singleSelect: true,
    fit: true,
    fitColumns: true
});
$.extend($.fn.layout.defaults,{
    fit: true
});
$.extend($.fn.combobox.defaults,{
    width: 200
});